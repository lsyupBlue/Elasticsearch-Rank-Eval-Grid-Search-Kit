# 템플릿 파라미터를 여러 조합으로 탐색합니다.
boost_title: [0.5, 1.0, 2.0, 4.0]
boost_content: [0.5, 1.0, 2.0]
operator: ["or", "and"]

# 추가 1) title/content 둘 다 맞아야 하는지
msm_fields: [1, 2]

# 추가 2) 쿼리 토큰 중 몇 개 이상 맞아야 하는지
# - "100%"는 사실상 AND 느낌, "50%"는 OR에서도 걸러짐
msm_terms: ["50%", "75%", "100%"]

# 추가 3) 오타/하이픈 변형 대응
fuzziness: ["0", "AUTO"]
